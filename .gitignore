# Build and codegen output
/out/
/generated/default/
/generated/default_base/
/build/

# Game content - never committed. The XEX and every asset belong to the
# rights holder; this project only ever reads a disc the user owns.
/assets/
/game/
*.xex
*.iso
*.bik
*.bnk
out/shots/
__pycache__/
*.pyc
/dlc/
/cache/
fable2_settings.cfg
fable2.toml
/rexglue-src/
/canary/

# DLL backups kept beside the build when the runtime pair is swapped
dll_backup_*/

# third-party AI upscaler engine + models (ACME's copy; not ours to commit)
tools/upscaler/

# The updater's download and staging folder beside the executable
/update/
